import { MovieUrlDirective } from "./movie-url.directive";

describe("MovieUrlDirective", () => {
  it("should create an instance", () => {
    const directive = new MovieUrlDirective();
    expect(directive).toBeTruthy();
  });
});
